# 5_mi_models

This directory is reserved for multiple-imputation model related artifacts.

Current contents:

- `r`: an empty placeholder file.

Notes:

- No script in `src/` currently reads from this directory directly.
- Use this folder for saved model-specific outputs only if you decide to persist artifacts outside the main `output/` structure.