# figures

This directory stores figure outputs generated by the analysis workflow.

Current subdirectory:

- `imputation/`: figures related to missingness assessment and imputation diagnostics.

Notes:

- Add additional figure categories here only when they represent a clear output grouping.