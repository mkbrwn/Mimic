# imputation

This directory contains figures related to missing data exploration and imputation diagnostics.

Current outputs include:

- missing data pattern figures
- imputation diagnostic figures

Notes:

- These files are generated by scripts such as `src/3_missingness.r`.
- Recreate figures from code instead of editing image files manually.