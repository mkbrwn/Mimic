# output

This directory contains generated analysis outputs.

Subdirectories:

- `figures/`: plots and image outputs.
- `tables/`: exported tabular results.

Notes:

- Files here are produced by scripts in `src/`.
- Regenerate outputs from source scripts when analysis code or inputs change.